
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper to check if user is signed in
    function isSignedIn() {
      return request.auth != null;
    }

    // Products: Anyone can read and list, only signed-in admins can write
    match /products/{productId} {
      allow read, list: if true;
      allow write: if isSignedIn();
    }

    // Posts: Anyone can read and list, only signed-in admins can write
    match /posts/{postId} {
      allow read, list: if true;
      allow write: if isSignedIn();
    }

    // Coupons: Anyone can read and list (for validation), only signed-in admins can write
    match /coupons/{couponId} {
      allow read, list: if true;
      allow write: if isSignedIn();
    }

    // Admin Roles: Only signed-in users can access their own role record
    match /roles_admin/{userId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;
    }
  }
}
